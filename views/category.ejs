<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= category %></title>
</head>

<body>
  <%- include('./partials/nav') %>

  <h1><%= category %></h1>
  <ul>
    <% menuChoices.forEach((item) => { %>
        <li><%= item.name %> $<%= item.price %> <br> <%= item.rating %> <br> "<%= item.details %>"</li>
    <% }) %>
  </ul>
</body>

</html>